# ORIGIN-FIRST-CROSSING-001

**Status:** executable synthetic specimen; not a live STATIC FIELD or Grace adapter. Source branch reference: STATIC FIELD feat/static-field-first-bell-001 @ 59d6adff7cc48de57340dc5c924bd26646105d41. Origin design: WORLDSEED docs/superpowers/specs/2026-09-21-origin-container-001-design.md.

Run `npm test` and `npm run demo:origin`. To play the local scene, serve the repository root with `python3 -m http.server 8000 --bind 127.0.0.1` and open `http://127.0.0.1:8000/origin/`. The scene stores a separate Origin-only session under `static-collective:origin-first-crossing-001`. Existing WORLDSEED author notebook, its storage key, and its document schema are untouched. Browser interaction uses the same ESM engine as the CLI.

The fixture is *shape-compatible*, not a fabricated live receipt. The STATIC FIELD branch already supplies FirstBellPlayReceipt and an unauthorized detected Gate after a traced Resonance relation. It does not yet provide a lawful OPEN/offer/departure adapter. Origin's adapter-local actions in this specimen have synthetic IDs; they do not claim to have been committed in STATIC FIELD. A missed clue cannot be converted into an authorized Gate. A real integration requires a separately reviewed source-owned export and source-owned departure contract.

The first destination is FOREIGN ROOM, not Grace/HOME. It has local events for a meal begun before arrival. An admitted party enters a world with prior history. The destination accepts the Anchor, a card *reference* and an unresolved Bell Thread; it rejects source Charge, holds an interpretation, transforms one origin-location reference into a new local reference, and withholds private memory and model-local capability. Neither the destination nor Origin inherits STATIC FIELD local authority. A card reference does not establish physical custody or PostEmahh'n grant. A hash witnesses deterministic local bytes, not source authenticity or divine meaning.

The Crossing record is sealed only after distinct human confirmation, source-local departure and destination-local arrival. An arrival failure retains the departure; recovery can complete without issuing the source departure twice. The source's Bell and Knock causes remain unresolved. The worldline stores the Crossing ref and party continuity, not a copy of both worlds.

A future Grace adapter can consume `full-measure.grace-day-receipt.v1` (replay witness, **not** signature) and `full-measure.portable-card.v1` (offered lineage, **not** PostEmahh'n custody) through its own destination admission, privacy and author-consent policies. It must not transfer Grace's entire session, day budget, relationships or local ranking automatically. The separately parked Grace/Mercy branch is not merged or activated by this fixture.
