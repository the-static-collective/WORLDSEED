# ORIGIN-TABLE-002 // THE WORLD ANSWERS

Status: bounded fictional fixture, stacked on ORIGIN-FIRST-CROSSING-001. No live donor adapter or Grace-world admission is claimed.

## Player proof

After the first Crossing is sealed, the half-finished meal is still in FOREIGN ROOM. The player can take an offered seat **or** stay at the door. Neither is graded as the moral choice. Either route permits LISTEN: someone in the local fiction mentions another house and an ordinary Tuesday. The player can inspect the reported address or let it rest. Inspection produces a **candidate Gate**, not a portal, admission, instruction, or destination consent.

The address points to the *existing* Full Measure `GRACE-001` author branch, pinned for developer provenance to `feature/grace-001-worldseed` commit `df1f20702012cd4e14a279e05d1a47a8255a40b9` and `specimens/grace-001/worldseed.json`. The **in-fiction report is fictional**; the code reference is not proof that Grace or Paula's world invited the party. Do not import Grace's Day Receipt, card lineage, world state, or personal data without a source-owned adapter and a destination-owned World Manifest / admission rule.

## Boundary

The new `table.*` events are appended only to FOREIGN ROOM's local history after a sealed first Crossing; STATIC FIELD's Bell, Porch, and departure are unchanged. The same party, Anchor and first Crossing receipt survive. Every accepted action has a scoped event ID and exact-payload replay protection; saved table history is replay-validated before display. The reported Gate remains DETECTED / unauthorized / unrequested with required Grace entry policy, world manifest, Foreign Room exit policy and separate human confirmation outstanding. No automatic cross-world mutation, private disclosure or authority transfer occurs.

Run `npm test`; serve the repository root and open `/origin/`. This is an additive sequel scene on the stacked Origin branch, not a change to the existing first-crossing expected receipt.
